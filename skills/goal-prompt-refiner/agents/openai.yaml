interface:
  display_name: "Goal Prompt Refiner"
  short_description: "Shape long work into one verified Goal"
  default_prompt: "Use $goal-prompt-refiner to inspect the project and produce one copy-ready /goal; when a material decision is needed, present interactive choices with the recommended option first instead of a static question."
policy:
  allow_implicit_invocation: true
