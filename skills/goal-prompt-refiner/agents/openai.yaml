interface:
  display_name: "Goal Prompt Refiner"
  short_description: "Shape long work into one verified Goal"
  default_prompt: "Use $goal-prompt-refiner to inspect the project and produce one copy-ready /goal with ordered phases; require the active phase gate to pass before advancing, and use interactive choices for material decisions instead of static questions."
policy:
  allow_implicit_invocation: true
