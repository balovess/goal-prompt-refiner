interface:
  display_name: "Goal Prompt Refiner"
  short_description: "Shape long work into one verified Goal"
  default_prompt: "Use $goal-prompt-refiner to understand the desired final outcome and turn this long-running project request into one explicit, project-grounded /goal prompt with resumable Goal memory, checkpoints, validation, and final acceptance gates."
policy:
  allow_implicit_invocation: true
